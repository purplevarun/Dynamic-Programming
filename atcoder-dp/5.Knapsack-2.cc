#include <iostream>
using namespace std;
#define int long long int
int32_t main () {
	int N, W;
	cin >> N >> W;
	int wt[N], val[N];
	int V = 0;
	for (int i=0;i<N;i++){
		cin >> wt[i] >> val[i];
		V += val[i];
	}

	
}
