#include <bits/stdc++.h>
using namespace std:
string LCS (string A, string B) {
    int N = A.size(), B.size(); 
    string lcs = "";
        
}
int superSequence(string A, string B) {
    int N = A.size(), B.size(); 
    
}
int main () {
    string A = "AGGTAB";
    string B = "GXTXAYB";
    cout << superSequence(A,B) << endl;    
}
