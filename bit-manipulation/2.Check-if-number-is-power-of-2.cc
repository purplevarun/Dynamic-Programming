#include <iostream>
using namespace std;
#define int int64_t
int32_t main () {
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	// N & N-1 has the same bits as N 
	// except the rightmost set bit
	
}